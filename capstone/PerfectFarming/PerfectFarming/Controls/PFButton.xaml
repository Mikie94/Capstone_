<UserControl x:Class="PerfectFarming.Controls.PFButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="35"
             x:Name="uc">
    <Grid>
        <Border x:Name="border" Tag="{Binding Path=Tag,ElementName=uc}" Background="Transparent" ToolTip="{Binding ToolTip,ElementName=uc}" Margin="0" BorderBrush="Transparent" MouseLeftButtonDown="Button_Click" >
            <Image Source="{Binding ImageSource,ElementName=uc}" Stretch="Uniform" Margin="5"/>
        </Border>
    </Grid>
</UserControl>
